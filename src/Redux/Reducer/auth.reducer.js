

const initValue = {
    isLoading: false,
    user: null,
    error: ''
}

export const authReducer = (state = initValue, action) => {
    switch (action.type) {
        default:
            return state;
    }

}